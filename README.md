Robin Andersson's personal web page. Built with [Metalsmith](http://metalsmith.io/).

```
# Install dependencies
$ npm install

# Scripts

prestart:
  install dependencies.
start:
  (Install dependencies and) Build to ./public.
```
